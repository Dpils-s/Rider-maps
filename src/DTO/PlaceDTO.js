class PlaceDTO {
    constructor(addressComponents, location, viewport, name) {
        this.addressComponents = addressComponents;
        this.location = location;
        this.viewport = viewport;
        this.name = name;
    }
}

export default PlaceDTO;
